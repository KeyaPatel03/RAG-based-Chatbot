# Web server
fastapi==0.111.4
uvicorn[standard]==0.32.1
# RAG / embeddings (runs locally on Render)
torch==2.3.0                          # used in query.py for embedding computation
transformers==4.40.1                   # AutoTokenizer, AutoModel for sentence-transformer
chromadb==0.5.5                       # vector DB
pysqlite3-binary==4.1.3               # chromadb needs a newer sqlite3 (critical on Render)
# HTTP + utils
requests==2.31.0
python-dotenv==1.0.1
# ChromaDB transitive deps (often need explicit pins on Render)
pydantic==2.13.2
